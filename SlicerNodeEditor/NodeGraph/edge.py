"""
Edge utilities — currently NodeGraphQt manages edges internally via its pipe
system. This module is a placeholder for future custom edge rendering
(animated flow, type-mismatch highlighting, etc.).
"""
